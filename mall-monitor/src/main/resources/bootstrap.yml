spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        shared-dataids: log.yaml,actuator.yaml
        endpoint:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: monitor
server:
  port: 9091
logging:
  level:
    root: debug